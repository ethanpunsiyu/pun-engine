# SETTINGS.txt
# One setting per line, `key = value`. # marks comments.
# Edit freely — this file is not AI-processed.

# Provider: "anthropic" or "openai".
# Requires ANTHROPIC_API_KEY or OPENAI_API_KEY in the environment respectively.
provider = openai

# Model names. Leave blank to use the provider-appropriate defaults:
#   anthropic → claude-opus-4-7 / claude-sonnet-4-6
#   openai    → gpt-5 / gpt-5-mini
# If you switch provider, either blank these out or set provider-matching names.
generator_model =
judge_model =

# How many exemplar puns the generator pulls from the corpus per batch.
exemplar_count = 3

# Min judge overall score for a pun to be admitted into the corpus.
# Raise this and existing sub-threshold entries get demoted to archive.txt.
threshold = 0.65

# Generation over-sampling factor (request n*over_factor candidates, return top n).
over_factor = 2

# How many corpus entries the learnlog summariser sees per refresh.
summary_sample_size = 40

# Max retries on 5xx / 529 (Overloaded) / connection errors.
max_api_retries = 6

# File locations (paths can be absolute or relative to the working dir).
corpus_path      = corpus.json
learnlog_path    = learnlog.json
archive_path     = archive.txt
model_puns_path  = MODEL.txt
conditions_path  = CONDITIONS.txt
